<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!-- 
        Nota: La base de datos application_service_db y el usuario ivan 
        deben ser creados manualmente por el administrador de la BD.
        
        Este changeset verifica que todo esté configurado correctamente.
    -->

    <!-- Verificar que la configuración inicial esté correcta -->
    <changeSet id="001-verify-initial-setup" author="ivan">
        <sql>
            -- Verificar que tenemos permisos para crear tablas en el schema public
            SELECT 1;
        </sql>
        <rollback>
            -- No hay rollback necesario para esta verificación
        </rollback>
    </changeSet>

</databaseChangeLog>
